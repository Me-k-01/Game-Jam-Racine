using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class grid_generate : MonoBehaviour
{
    //[SerializeField] private Tile case;
    // Start is called before the first frame update
    void Start()
    {
       // generateGrid();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    
    /*void generateGrid(){
        for(int i=0;i<6;i++){
            for(int j=0;j<6;j++){
                var tile = Instantiate(case,new Vector3(0,i,j), Quaternion.identity);
                tile.name=$"case {i} {j}";
            }
        }
    }*/
}
